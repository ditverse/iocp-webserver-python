## Overview
This repository contains a simple asynchronous web server implemented in Python.
The server uses I/O multiplexing with `select.select()` to handle multiple client connections efficiently.
This project was developed as part of the Network Programming practicum.
